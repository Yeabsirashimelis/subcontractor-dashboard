services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: subcontractor
      POSTGRES_PASSWORD: subcontractor
      POSTGRES_DB: subcontractor_dashboard
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
